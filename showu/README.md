# ShowU - Social Media App

## 🚀 Tech Stack
- FastAPI (Python)
- PostgreSQL + SQLAlchemy
- JWT Authentication
- Redis + Celery (background jobs)
- Docker + docker-compose

## 📦 Setup
1. Clone repo and create `.env` from `.env.example`.
2. Build and run containers:
   ```bash
   docker-compose up --build